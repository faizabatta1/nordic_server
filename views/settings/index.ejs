<!DOCTYPE html>
<html>
<head>
    <title>Settings</title>
    <style>
        /* Custom CSS */
        body {
            font-family: Arial, sans-serif;
            background-color: #f7f7f7;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background-color: #ffffff;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }
        .section {
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 1px solid #e0e0e0;
        }
        .section h2 {
            margin: 0;
            padding-bottom: 10px;
            border-bottom: 2px solid #007bff;
            color: #333;
        }
        label {
            font-weight: bold;
            display: block;
            margin-bottom: 5px;
        }
        input[type="text"],
        input[type="password"],
        textarea {
            width: 100%;
            padding: 10px;
            border: 1px solid #e0e0e0;
            border-radius: 5px;
            margin-bottom: 15px;
            color: #333;
        }
        textarea {
            resize: vertical;
            height: 150px;
        }
        button {
            padding: 10px 20px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        button:hover {
            background-color: #0056b3;
        }
        .preview {
            border: 1px solid #e0e0e0;
            padding: 15px;
            border-radius: 5px;
            background-color: #f9f9f9;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="title">Settings</h1>
        
        <div class="section">
            <h2 class="section-title">Admin Credentials</h2>
            <form action="/updateAdminCredentials" method="post">
                <label for="adminUsername" class="label">Username</label>
                <input type="text" id="adminUsername" name="adminUsername" class="input" value="USERNAME" required>
                <label for="adminPassword" class="label">Password</label>
                <input type="password" id="adminPassword" name="adminPassword" class="input" value="PASSWORD" required>
                <button type="submit" class="button">Save</button>
            </form>
        </div>

        <div class="section">
            <h2 class="section-title">Email Template</h2>
            <p class="helper-text">Use placeholders for dynamic content: {private}, {board}, {kilometers}</p>

            <form action="/updateEmailTemplate" method="post">
                <label for="emailSubject" class="label">Subject</label>
                <input type="text" id="emailSubject" name="emailSubject" class="input" value="xx@gmail.com" required>
                <label for="emailText" class="label">Text</label>
                <textarea id="emailText" name="emailText" class="input input-textarea" required>
                  this is a simple template my car number is {board} and it exceeded {kilometers}
                </textarea>
                <button type="submit" class="button button-primary">Save</button>
            </form>
            <div class="preview">
                <h4 class="preview-title">Subject: Car {private}</h4>
                <p class="preview-text">this is a simple template my car number is Ec65656 and private number EL 01 and it exceeded service kilometer 2000</p>
            </div>
        </div>

        <div class="section">
            <h2 class="section-title">SMS Template</h2>
            <form action="/updateSmsTemplate" method="post">
                <label for="smsText" class="label">Text</label>
                <textarea id="smsText" name="smsText" class="input input-textarea" required>
                  this is a simple template my car number is {board} and it exceeded {kilometers}
                </textarea>
                <button type="submit" class="button button-primary">Save</button>
            </form>
            <div class="preview">
                <p class="preview-text">this is a simple template my car number is Ec65656 and private number EL 01 and it exceeded service kilometer 2000</p>
            </div>
        </div>
    </div>
</body>
</html>
